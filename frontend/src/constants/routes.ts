export const ROUTES = {
  LOGIN: "/login",
  DASHBOARD: "/dashboard",
  SALES: "/sales",
  CUSTOMERS: "/customers",
  REPORTS: "/reports",
} as const;

export const STORAGE_KEYS = {
  ACCESS_TOKEN: "auth_access_token",
} as const;
